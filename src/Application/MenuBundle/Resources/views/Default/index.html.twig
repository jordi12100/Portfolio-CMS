			<ul>
			    {% for item in items %}
			    	{% if item.isRoute %}
			        	<li><a href="{{ path(item.link) }}">{{ item.name }}</a></li>	
			    	{% else %}
			    		<li><a href="{{ item.link }}">{{ item.name }}</a></li>	
			        {% endif %}
			    {% endfor %}
    
			</ul>